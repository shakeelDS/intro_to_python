# Use the paramater and argument  normalize = True to return proportions

# Show the help documentation

?pd.Series.value_counts()

# modify the previous code
titanic["pclass"].value_counts(normalize=True)
