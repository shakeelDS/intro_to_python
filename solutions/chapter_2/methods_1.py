# Use the `.pop()` function on the `parrots` list to remove the value `Norwegian Blue

parrots.pop(4)

# Look at the list to see what's removed

parrots

# Note - if you didn't order your list; this will remove a different element!